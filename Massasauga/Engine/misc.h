#ifndef MISC_H
#define MISC_H

double Round( double value );
double ForceProgress(double value); //If number is not zero and between -1 and 1, 
                                    //round towards neg and pos infinity respectively (make progress)

#endif